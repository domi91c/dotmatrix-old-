set -o vi
# export EDITOR=vim
# export VISUAL=vim

# override for spacemacs
# export EDITOR="ITERM_24BIT=1 emacsclient -c -a ''"
# export VISUAL="ITERM_24BIT=1 emacsclient -c -a ''"
# switching back to vim temporarily
export EDITOR="vim"
export VISUAL="vim"
